{% layout "server/layouts/base.liquid" %}

{% block content %}
  <h1 class="h1">{{ items.number }}</h1>
  <p>test ites</p>
  {% comment %} <img
    src="{{ item.image.src }}"
    alt="{{ item.image.alt }}"
    width="{{ item.image.width }}"
    height="{{ item.image.height }}" /> {% endcomment %}
{% endblock %}